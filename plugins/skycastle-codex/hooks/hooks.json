{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "PLUGIN_ROOT=\"${CODEX_PLUGIN_ROOT:-}\"; if [ -z \"$PLUGIN_ROOT\" ] || [ ! -f \"$PLUGIN_ROOT/hooks/skycastle-enforce.js\" ]; then exit 0; fi; node \"$PLUGIN_ROOT/hooks/skycastle-enforce.js\"",
            "statusMessage": "Checking skycastle..."
          }
        ]
      },
      {
        "matcher": "mcp__.*__skyline_run$",
        "hooks": [
          {
            "type": "command",
            "command": "PLUGIN_ROOT=\"${CODEX_PLUGIN_ROOT:-}\"; if [ -z \"$PLUGIN_ROOT\" ] || [ ! -f \"$PLUGIN_ROOT/hooks/skycastle-enforce.js\" ]; then exit 0; fi; node \"$PLUGIN_ROOT/hooks/skycastle-enforce.js\"",
            "statusMessage": "Checking skycastle..."
          }
        ]
      }
    ]
  }
}
