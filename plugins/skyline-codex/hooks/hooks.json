{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read",
        "hooks": [
          {
            "type": "command",
            "command": "PLUGIN_ROOT=\"${CODEX_PLUGIN_ROOT:-}\"; if [ -z \"$PLUGIN_ROOT\" ] || [ ! -f \"$PLUGIN_ROOT/hooks/skyline-enforce.sh\" ]; then exit 0; fi; bash \"$PLUGIN_ROOT/hooks/skyline-enforce.sh\" read",
            "statusMessage": "Checking skyline..."
          }
        ]
      },
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "PLUGIN_ROOT=\"${CODEX_PLUGIN_ROOT:-}\"; if [ -z \"$PLUGIN_ROOT\" ] || [ ! -f \"$PLUGIN_ROOT/hooks/skyline-enforce.sh\" ]; then exit 0; fi; bash \"$PLUGIN_ROOT/hooks/skyline-enforce.sh\" edit",
            "statusMessage": "Checking skyline..."
          }
        ]
      },
      {
        "matcher": "Grep",
        "hooks": [
          {
            "type": "command",
            "command": "PLUGIN_ROOT=\"${CODEX_PLUGIN_ROOT:-}\"; if [ -z \"$PLUGIN_ROOT\" ] || [ ! -f \"$PLUGIN_ROOT/hooks/skyline-enforce.sh\" ]; then exit 0; fi; bash \"$PLUGIN_ROOT/hooks/skyline-enforce.sh\" grep",
            "statusMessage": "Checking skyline..."
          }
        ]
      },
      {
        "matcher": "Glob",
        "hooks": [
          {
            "type": "command",
            "command": "PLUGIN_ROOT=\"${CODEX_PLUGIN_ROOT:-}\"; if [ -z \"$PLUGIN_ROOT\" ] || [ ! -f \"$PLUGIN_ROOT/hooks/skyline-enforce.sh\" ]; then exit 0; fi; bash \"$PLUGIN_ROOT/hooks/skyline-enforce.sh\" glob",
            "statusMessage": "Checking skyline..."
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "PLUGIN_ROOT=\"${CODEX_PLUGIN_ROOT:-}\"; if [ -z \"$PLUGIN_ROOT\" ] || [ ! -f \"$PLUGIN_ROOT/hooks/skyline-enforce.sh\" ]; then exit 0; fi; bash \"$PLUGIN_ROOT/hooks/skyline-enforce.sh\" bash",
            "statusMessage": "Checking skyline..."
          }
        ]
      }
    ]
  }
}
